const INITIAL_STATE = {
    name: "",
    email: "",
    organization: "",
};

const smeReducer = (state = INITIAL_STATE, action) => {
    switch (action) {
        default:
            return state;
    }
};

export default smeReducer;
